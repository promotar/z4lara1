# Local verification files

Environment-specific verification scripts, credentials, database targets, and
server paths are intentionally excluded from Git. Keep local scripts in this
directory and commit only sanitized `*.example.*` templates.
