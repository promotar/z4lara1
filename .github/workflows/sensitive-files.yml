name: Sensitive file guard

on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject tracked credentials and runtime environments
        run: bash scripts/verify-no-sensitive-files.sh
